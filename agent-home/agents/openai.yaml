interface:
  display_name: "Agent Home"
  short_description: "Keep private state across agent sessions"
  default_prompt: "Use $agent-home to resolve a durable private directory for this agent's persistent state."
